<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
 
 <!-- 170912 create = limitationOne  -->
 
<mapper namespace="com.publicMapper.IHotelMemberMapper">
	
	<!-- ******* login ******** -->
	<!-- loginEmail -->
	<select id="loginEmail" parameterType="com.model.HotelUserVo" resultType="com.model.HotelUserVo">
		SELECT * FROM tbl_hotel_user_info WHERE id=#{id}
	</select>
</mapper>